import type { FC } from "react";
import WikiProseBlock from "@/components/WikiProseBlock";

const DynamicLight: FC = () => {
  return (
    <WikiProseBlock>
      <h1>Уникальные предметы</h1>
    </WikiProseBlock>
  );
};

export default DynamicLight;
